/**
 * Interfaz que define la estructura de los elementos ToDo.
 */
export interface ToDo {
    // Identificador del ToDo.
    id: string;
    // Nombre del ToDo.
    name: string;
    // Estado del ToDo.
    status: boolean;
}
